package service;
/*
 * package service;
 * 
 * import javax.servlet.http.HttpServletRequest; import
 * javax.servlet.http.HttpServletResponse;
 * 
 * import di.M_Action; import model.CenterDAO; import model.CenterDTO;
 * 
 * public class Insert implements M_Action {
 * 
 * @Override public void execute(HttpServletRequest request, HttpServletResponse
 * response) throws Exception { int dto = new
 * CenterDAO().insert(request.getParameter("pname"),
 * request.getParameter("title"),request.getParameter("content"));
 * request.setAttribute("data", new CenterDAO().detail(Integer.toString(dto)));
 * System.out.println(dto); }
 * 
 * }
 */